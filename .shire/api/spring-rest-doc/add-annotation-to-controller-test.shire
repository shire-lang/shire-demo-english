---
name: "Add @AutoConfigureRestDocs annotation to the test class"
variables:
  "code": /any/ { cat($input) }
onStreamingEnd: { parseCode | saveFile($input, $output) }
---

To add Spring RestDocs support to the following code, do the following:

1. Add the `@AutoConfigureRestDocs(outputDir = "build/snippets")` annotation to the test class.
2. Add the corresponding import statement: `import org.springframework.boot.test.autoconfigure.restdocs.AutoConfigureRestDocs`.

Requirements:

1. Do not modify the task code, only add the corresponding annotation.

File path:

$input

Code:

$code

Please return only the final code for my convenience.
